{
  "name": "@panabarbero/constants",
  "type": "module",
  "version": "0.0.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "generate": "dotenv -e ../../.env -- yarn @better-auth/cli generate --output ../db/src/auth-schema.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "peerDependencies": {
    "expo-router": "~5.1.4"
  }
}
