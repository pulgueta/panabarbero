[phases.install]
    cmds = ["bun install --omit=dev"]

[build]
    builder = "railpack"
    buildCommand = "bun run build"

[deploy]
    startCommand = "bun run start"
    restartPolicyType = "never"
