{
  "name": "panabarbero",
  "private": true,
  "engines": {
    "bun": ">=1.2.19"
  },
  "packageManager": "bun@1.2.19",
  "scripts": {
    "build": "turbo run build",
    "clean": "git clean -xdf node_modules",
    "clean:workspaces": "turbo run clean",
    "auth:generate": "bun -F @panabarbero/auth generate",
    "db:push": "turbo -F @panabarbero/db push",
    "db:studio": "turbo -F @panabarbero/db studio",
    "dev": "turbo watch dev --continue",
    "dev:next": "turbo watch dev -F @panabarbero/nextjs...",
    "lint": "biome lint --write --unsafe .",
    "format": "biome format --write .",
    "ui-add": "turbo run ui-add",
    "android": "expo run:android",
    "ios": "expo run:ios"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "turbo": "^2.5.5",
    "typescript": "^5.9.2"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
